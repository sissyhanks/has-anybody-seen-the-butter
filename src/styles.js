import { makeStyles } from "@mui/styles";

export default makeStyles((theme) => ({
  nav: {
    color: '#6D466B',
    '&:hover': {
      color: '#DD99BB',
    }
  },

  bernie: {
    color: '#F9C80E',
    '&:hover': {
      color: '#DD99BB',
    }
  }
}));